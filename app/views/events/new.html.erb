<h2><%= @page_title %></h2>

<%= form_for @event, :as => :event, :url => {:controller => :events, :action => :new, :id => @top_category}, :method => :post, :html => {:class => "form-horizontal"} do |f| %>
    <%= render :partial => "events/form", :locals => {:f => f} %>
<% end %>
